apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "test2.fullname" . }}
  labels:
    {{- include "test2.labels" . | nindent 4 }}
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: {{ include "test2.fullname" . }}
    weight: 100
  wildcardPolicy: None
